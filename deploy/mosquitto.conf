# WaffleWeather Mosquitto configuration
# Installed to /etc/mosquitto/conf.d/waffleweather.conf

# MQTT listener
listener 1883

# WebSocket listener (for browser clients)
listener 9001
protocol websockets

# Allow anonymous connections (local network only)
allow_anonymous true
